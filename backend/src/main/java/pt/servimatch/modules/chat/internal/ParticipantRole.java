package pt.servimatch.modules.chat.internal;

/** Papel do utilizador autenticado numa {@code Conversation} (autorização em {@link ChatService}). */
enum ParticipantRole {
    CUSTOMER,
    PROVIDER
}
