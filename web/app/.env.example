# Nenhum segredo aqui — apenas configuração pública de build.
# Em desenvolvimento, o Vite faz proxy de /api e /auth para o BFF (ver vite.config.ts).
# Só precisas de definir isto se o BFF não estiver em localhost:4000.
VITE_BFF_ORIGIN=http://localhost:4000
