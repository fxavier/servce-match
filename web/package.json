{
  "name": "servimatch-web",
  "private": true,
  "version": "0.1.0",
  "description": "ServiMatch — site público (site/) + BFF Node/Express (bff). Ver README.md.",
  "workspaces": [
    "site",
    "bff"
  ],
  "scripts": {
    "dev:site": "npm run dev --workspace site",
    "dev:bff": "npm run dev --workspace bff",
    "build": "npm run build --workspace site && npm run build --workspace bff",
    "lint": "npm run lint --workspace site && npm run lint --workspace bff",
    "typecheck": "npm run typecheck --workspace site && npm run typecheck --workspace bff",
    "test": "npm run test --workspace site && npm run test --workspace bff",
    "test:e2e": "playwright test --config e2e/playwright.config.ts",
    "generate:api": "npm run generate:api --workspace site"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "oauth2-mock-server": "^9.1.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
